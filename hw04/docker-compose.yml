version: '3.8'
services:
  warehouse:
    image: postgres:18
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin1234
      POSTGRES_DB: taxi_rides_ny
    ports:
      - "5432:5432"
    volumes:
      - ./postgres_data:/var/lib/postgresql/data